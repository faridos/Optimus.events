<form action="{{ path('new_album_club',{ 'id' : entity.id }) }}" {{ form_enctype(form) }} method="post" class="form-horizontal0 form-groups-bordered">
            {{ form_errors(form) }}
            <div class="well">
                <div class="form-group">
                    <div class="col-md-12 form-group">
                        <div class="col-md-3">
                            <label class="control-label">Nom</label>
                        </div>
                        <div class="col-md-9">
                            {{ form_widget(form.nom) }}
                        </div>
                    </div>
                    <div class="col-md-12 form-group">
                        <div class="col-md-3">
                            <label class="control-label">Visibilité</label>
                        </div>
                        <div class="col-md-9">
                            {{ form_widget(form.privacy) }}
                        </div>
                    </div>
                </div>
                <button type="submit" class="btn btn-success">
                    <i class="entypo-check"></i>Enregistrer
                </button>
                {{ form_widget(form._token) }}
            </div>
        </form>