{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Seance</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Jourseance</th>
                <td>{{ entity.jourseance }}</td>
            </tr>
            <tr>
                <th>Heuredebut</th>
                <td>{{ entity.heuredebut }}</td>
            </tr>
            <tr>
                <th>Heurefin</th>
                <td>{{ entity.heurefin }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('seance') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('seance_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
