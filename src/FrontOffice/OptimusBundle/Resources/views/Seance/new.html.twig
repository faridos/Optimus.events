{% extends '::base.html.twig' %}

{% block body -%}
     <form action="{{ path("seance_create", {'id': entity.id, 'idp': program.id }) }}" {{ form_enctype(form) }} method="post" class="form-horizontal0 form-groups-bordered">
                                    <br>
                                    {{ form_errors(form) }}
                    <div class="well">
                     


                        <div class="form-group">

                            <div class="col-md-12 form-group">
                                <div class="col-md-3">
                                    <label class="control-label">Nom :</label>
                                </div>
                                <div class="col-md-9">
                                    {{ form_widget(form.nom) }}
                                </div>
                            </div>

                            <div class="col-md-12 form-group">
                                <div class="col-md-3">
                                    <label class="control-label">jour:</label>
                                </div>
                                <div class="col-md-9">
                                    {{ form_widget(form.description) }}
                                </div>
                            </div>
                            <div class="col-md-12 form-group">
                                <div class="col-md-3">
                                    <label class="control-label">Heure Debut :</label>
                                </div>
                                <div class="col-md-9">
                                    {{ form_widget(form.datedebut) }}
                                </div>
                            </div>
 <div class="col-md-12 form-group">
                                <div class="col-md-3">
                                    <label class="control-label">Heure Fin :</label>
                                </div>
                                <div class="col-md-9">
                                    {{ form_widget(form.datefin) }}
                                </div>
                            </div>



                        </div>

                    <button type="submit" class="btn btn-blue">Enregistrer</button>
                                  {{ form_widget(form._token) }}
                    </div>
                
                    </form>
{% endblock %}
